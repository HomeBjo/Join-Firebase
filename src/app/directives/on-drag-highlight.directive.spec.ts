import { OnDragHighlightDirective } from './on-drag-highlight.directive';

describe('OnDragHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new OnDragHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
